export interface Sector {
    columns: number;
    created: string;
    creator: string;
    layers: Map<string, boolean>;
    mapLocked: boolean;
    name: string;
    rows: number;
    updated: string;
    attributes: any;
}
